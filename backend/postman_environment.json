{
  "id": "cloud-storage-local-env",
  "name": "Cloud Storage - Local",
  "values": [
    {
      "key": "baseUrl",
      "value": "http://localhost:8000",
      "type": "default",
      "enabled": true
    },
    {
      "key": "userId",
      "value": "REPLACE_WITH_A_REAL_USER_UUID",
      "type": "default",
      "enabled": true,
      "description": "Real auth isn't built yet (see app/core/deps.py) - every request authenticates via the X-User-Id header. Create a user row first (see README 'Setting up Supabase' step 5) and paste its id here."
    }
  ],
  "_postman_variable_scope": "environment"
}
