{
  "id": "cloud-storage-local-env",
  "name": "Cloud Storage - Local",
  "values": [
    {
      "key": "baseUrl",
      "value": "http://localhost:8000",
      "type": "default",
      "enabled": true
    },
    {
      "key": "userId",
      "value": "REPLACE_WITH_A_REAL_USER_UUID",
      "type": "default",
      "enabled": true,
      "description": "Dev-only fallback (see app/core/deps.py), only works when the backend's ENV != 'production'. Prefer calling POST /auth/register or /auth/login instead - Postman stores the resulting session cookies automatically and this variable becomes unnecessary."
    }
  ],
  "_postman_variable_scope": "environment"
}
